name: Rubocop

on: [push]

jobs:
  build:
    timeout-minutes: 10
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Install dependencies
        run: |
          gem install bundler
          bundle install --local

      - run: bin/rubocop
